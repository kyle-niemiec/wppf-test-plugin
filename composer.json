{
    "require": {
        "kyle-niemiec/wppf-update-helper": "^1.0",
        "kyle-niemiec/wp-plugin-framework": "^1.2"
    },
    "scripts": {
        "build": [
            "rm -rf ./build",
            "mkdir -p ./build/src",
            "composer install",
            "rsync -r --exclude-from=.distignore ./ ./build/src",
            "zip -r ./build/wppf-test-plugin.zip ./build/src",
            "(cd ./build/src ; zip -r ../wppf-test-plugin.zip ./)"
        ]
    }
}
